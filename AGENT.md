# Lessons Learned

## Project Rules

### DO NOT run builds, checks, or the application
- The user builds and tests the app themselves using `run.sh`
- Never execute `haxe`, `npm run`, `run.sh`, or any build/check commands
- Only make code changes and let the user verify
